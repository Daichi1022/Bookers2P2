<h1>New book</h1>
  <%= form_with model: book do |f| %>
    <p>Title</p>
      <%= f.text_field :title %>
    <p>Option</p>
      <%= f.text_area :body %>
    <%= f.submit 'Create Book' %>
  <% end %>